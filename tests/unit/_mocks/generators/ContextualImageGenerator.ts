"use strict";

import { ContextualImage } from "@/core/image/ContextualImageService";

export function generateContextualImage(): ContextualImage {
    return {
        color: "#fff",
        src: "dummy.url"
    };
}
